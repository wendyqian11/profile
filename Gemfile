source "https://rubygems.org"

# This gem bundles everything GitHub Pages uses to build your site, so a
# local preview matches the live site. You only need this if you want to
# preview the site on your own computer before pushing (optional).
gem "github-pages", group: :jekyll_plugins

# Windows / JRuby compatibility helpers (harmless on other systems).
gem "webrick"
